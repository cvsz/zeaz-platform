name: rotate-secrets
on:
  schedule: [{cron: '0 3 1 * *'}]
  workflow_dispatch:
jobs:
  rotate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: echo "sops+age rotation"
