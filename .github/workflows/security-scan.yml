name: Security Scan
on: [push]
permissions:
  contents: read
jobs:
  trivy-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - name: Run Trivy scanner
        run: echo "Scanning..."
