name: security-scan
on: [pull_request]
concurrency: { group: sec-${{ github.ref }}, cancel-in-progress: true }
jobs:
  scan:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: echo "trivy + sbom + cosign hooks"
