name: drift-detect
on:
  schedule: [{cron: '17 */6 * * *'}]
  workflow_dispatch:
concurrency: { group: drift-${{ github.ref }}, cancel-in-progress: false }
jobs:
  drift:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: echo "drift detection pipeline"
