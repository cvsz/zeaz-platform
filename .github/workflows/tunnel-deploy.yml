name: tunnel-deploy
on: { workflow_dispatch: {} }
concurrency: { group: tunnel-${{ github.ref }}, cancel-in-progress: false }
jobs:
  deploy:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: echo "deploy cloudflared"
