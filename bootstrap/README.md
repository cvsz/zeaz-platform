# Bootstrap

This directory contains phase F1 bootstrap assets for local and CI environments.

Current bootstrap entrypoint:

- `scripts/validate.sh` for deterministic offline runtime validation.

Future phases should add deterministic bootstrap manifests and environment overlays in this directory.
