# cloudflare-api-shield

This module currently provisions a Cloudflare API traffic logging ruleset (`http_request_firewall_custom` phase) to support API inventory and onboarding workflows.

> Note: This is not full Enterprise API Shield schema/mTLS enforcement.
