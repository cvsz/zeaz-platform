def api_evolution_proposals() -> list[dict]:
    return [
        {
            "proposal": "version sensitive endpoints",
            "impact": "low",
            "approval_required": True,
        }
    ]
