[Unit]
Description=zsticker Bot
Requires=docker.service
After=docker.service

[Service]
Type=simple
WorkingDirectory=/opt/zsticker
ExecStart=/usr/bin/docker compose up
ExecStop=/usr/bin/docker compose down
Restart=always
User=root
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
