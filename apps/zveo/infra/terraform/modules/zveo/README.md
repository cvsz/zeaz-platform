# zVEO Terraform Module Boundary

This directory is reserved for extracting the root Terraform resources into reusable modules without changing existing state addresses. Use `terraform state mv` during the migration window after applying the root configuration successfully.
