import { EmptyState } from "../../components/states";
export default function AssetsPage() { return <main className="p-8"><h1 className="text-2xl">Assets</h1><EmptyState title="Select a workflow" message="Asset previews are available on each workflow detail page." /></main>; }
