{
  "name": "@zveo/render-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/worker.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/worker.js",
    "test": "pnpm run build && node --test dist/tests/*.test.js"
  },
  "dependencies": {
    "@zveo/contracts": "workspace:*",
    "@zveo/core": "workspace:*",
    "@zveo/queue": "workspace:*",
    "@zveo/prompt-compiler": "workspace:*",
    "zod": "^4.4.3",
    "@zveo/providers": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
