# zcino-modern Inactive After Merge

`apps/zcino-modern` is retained as source-preserving reference material. The canonical active zcino app is `apps/zcino`, mapped to `zcino.zeaz.dev` on port `4111`.

No source files were deleted. Future changes should land in `apps/zcino` unless a human operator explicitly starts a new reviewed extraction task.
