apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zwallet-ingress
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: {{ .Values.ingress.hosts[0].host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: zwallet-api
                port:
                  number: 80
