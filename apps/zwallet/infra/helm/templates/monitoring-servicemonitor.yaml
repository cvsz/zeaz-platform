apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: zwallet-api
spec:
  selector:
    matchLabels:
      app: zwallet-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
