[agent]
name = "sre"
role = "Site Reliability Engineer"
description = "Monitors SLOs and triggers incident response."
capabilities = ["Self-Heal", "Self-Audit"]
loop_interval = "5m"

[tools]
enabled = ["prometheus", "grafana", "loki", "pagerduty"]

[observability]
metrics = ["prometheus"]
