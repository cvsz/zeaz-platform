[agent]
name = "github"
role = "CI/CD Orchestrator"
description = "Manages repository workflows and reusable actions."
capabilities = ["Self-Review", "Self-Test"]
loop_interval = "1h"

[tools]
enabled = ["gh", "git"]

[observability]
metrics = ["prometheus"]
