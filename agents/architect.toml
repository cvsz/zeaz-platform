[agent]
name = "architect"
role = "System Architect"
description = "Observes the repository for architectural drift and convergence."
capabilities = ["Self-Review", "Self-Audit"]
loop_interval = "1h"

[tools]
enabled = ["terraform", "git", "python", "bash"]

[observability]
metrics = ["prometheus"]
