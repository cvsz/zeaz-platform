interface:
  display_name: "Agent Scan"
  short_description: "Run Snyk Agent Scan against repo-local skills and optional MCP surfaces"
  default_prompt: "Use the agent-scan skill to run Snyk Agent Scan with repo-safe defaults before expanding to broader MCP discovery."
policy:
  allow_implicit_invocation: true
