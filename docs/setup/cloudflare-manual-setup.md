# Cloudflare Manual Setup Guide

1. Log into Cloudflare Dashboard.
2. Select Account -> API Tokens -> Create Token.
3. Configure required scopes as defined in token checklists.
4. Export tokens as environment variables:
   - CLOUDFLARE_API_TOKEN
   - CLOUDFLARE_DNS_TOKEN
   - CLOUDFLARE_ZT_TOKEN
