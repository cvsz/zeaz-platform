model_list:
  - model_name: "gpt-4o"
    litellm_params:
      model: "openai/gpt-4o"
  - model_name: "grok-beta"
    litellm_params:
      model: "xai/grok-beta"

general_settings:
  master_key: "os.environ/LITELLM_MASTER_KEY"

litellm_settings:
  drop_params: true
  set_verbose: true

# Fallback and routing configuration
fallbacks: []