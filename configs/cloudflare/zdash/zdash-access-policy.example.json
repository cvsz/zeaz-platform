{
  "description": "zDash Access policy template — dry-run only",
  "policy": {
    "name": "zDash Admin Access",
    "decision": "allow",
    "include": [
      {
        "email_domain": {
          "domain": "zeaz.dev"
        }
      }
    ],
    "exclude": [],
    "require": [],
    "bypass_rules": [],
    "session_duration": "24h"
  },
  "notes": [
    "Replace email_domain with your identity provider group or email list",
    "Bypass rules disabled by default — enable only for public read-only access",
    "Apply via Terraform or Cloudflare dashboard"
  ]
}
