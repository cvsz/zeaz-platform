name: drift-detection
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
jobs:
  drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - run: cloudflare-platform/scripts/drift-detect.sh dev
