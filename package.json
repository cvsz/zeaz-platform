{
  "name": "zeaz-platform",
  "version": "1.0.0",
  "private": true,
  "description": "ZeaZ Platform OMEGA Monorepo",
  "scripts": {
    "lint": "pnpm -r run lint",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "overrides": {
    "postcss": "8.5.15",
    "js-yaml": "4.1.0",
    "esbuild": "0.28.1",
    "prismjs": "1.29.0"
  }
}
