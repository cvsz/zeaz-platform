services:
  litellm:
    extends:
      file: ../infra/litellm/docker-compose.yaml
      service: litellm
  trading-ai:
    depends_on:
      litellm:
        condition: service_healthy
    environment:
      - LITELLM_BASE_URL=http://litellm:4000
      - LITELLM_API_KEY=${LITELLM_MASTER_KEY}